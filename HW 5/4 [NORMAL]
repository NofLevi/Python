def makeList(n):
    mat = []
    for i in range(n+1):
        tempmat = []
        for j in range(1,i+1):
            tempmat.append(j)
        mat.append(tempmat)
    return mat

n = int(input("Enter the n:"))
print(makeList(n))
